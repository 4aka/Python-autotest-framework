# https://github.com/orgs/community/discussions/42133

name: Automation project

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    # environment: ${{ github.event.inputs.TARGET_ENV || 'test' }}

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      env:
        BROWSER_NAME: ${{ vars.BROWSER_NAME }}
        BASE_URL: ${{ vars.BASE_URL }}
        UI_BASE_URL: ${{ vars.UI_BASE_URL }}
        LOG_LEVEL: ${{ vars.LOG_LEVEL }}
        API_KEY: ${{ vars.API_KEY }}
        PHONE_NUMBER: ${{ vars.PHONE_NUMBER }}
        NP_BASE_URL: ${{ vars.NP_BASE_URL }}
      run: |
        echo ${{ vars.ENVIRONMENT_NAME }}
        echo ${{ vars.BROWSER_NAME }}
        echo ${{ vars.BROWSER_NAME }}
        pytest
